@model IEnumerable<Ajanvarausprojekti.ViewModels.ajatListaData>

@*@section Scripts {
        <script type="text/javascript">
            $(function () {
                $(".valitseAika").click(function () {
                    let aika_id = $(this).data("aikaId");
                    alert("Klikkasit aikaa, jonka aika-id on " + aika_id);
                });
            });
        </script>

    }*@


<table class="table table-sm table-hover caption">
    <thead class="thead-dark bg-black">
        <tr>
            <th scope="col">Opettajan nimi</th>
            <th scope="col">Päivämäärä</th>
            <th scope="col">Kellonaika</th>
            <th scope="col">Tapaamisen kesto</th>
            <th scope="col"></th>
            <th scope="col">Aika_id (käytössä vain testailuvaiheessa)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {

            <tr>
                <td>@item.Etunimi @item.Sukunimi</td>
                <td>@item.Alkuaika.Value.ToString("dd.MM")</td>
                <td>@item.Alkuaika.Value.ToShortTimeString()</td>
                <td>@item.Kesto min</td>
                <td @*class="valitseAika" data-aikaId="@item.aika_id"*@><a href="#" class="btn btn-primary">Valitse aika</a></td>
                <td>@item.aika_id</td>
            </tr>
        }
    </tbody>

</table>

